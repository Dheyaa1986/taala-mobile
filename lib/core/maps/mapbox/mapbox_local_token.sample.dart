/// Sample Mapbox token file — committed empty for GitHub.
///
/// Local dev: paste your `pk.` token here **without committing**,
/// or use `--dart-define=MAPBOX_ACCESS_TOKEN=pk.xxx`.
const String kMapboxLocalToken = '';
